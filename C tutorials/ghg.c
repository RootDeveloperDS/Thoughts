#include <stdio.h>

int main()
{
	/* your code */
	int number;
	float float_;
	printf("How many days in the week:");
	scanf("%d", &number);
	printf("what is the value of pi to two points:");
	scanf("%f",&float_);
	printf("there are %d days in the week",number);
	printf("the value of pi to two points:%f",float_);
	printf("pi value is %f",float_);
	return 0;
}